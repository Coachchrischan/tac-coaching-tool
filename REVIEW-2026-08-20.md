
=== MUSTNOW (5) ===
[critical] #3 TV board, grids and CSV render nothing for ESD, Hyrox and Game Day
   area: src/tabs/tv/TvPage.tsx:105-107; src/tabs/programming/EditableGrid.tsx:65-98; src/lib/exportCsv.ts:43
   fix: Add a circuit branch to TvPage rendering each CircuitBlock heading, lines and restAfter as board cards, emit circuit text as CSV rows, and give the Block and Phase grids a read-only circuit summary.
   votes: mustFix=5 disputed=0 discs=ops/sc/qa
[critical] #32 Session carries two mutually exclusive payloads with no discriminator, so writers drop circuits
   area: src/types/documents.ts:99-108; src/tabs/programming/ProgrammingTab.tsx:134, 168-194
   fix: Make Session a discriminated union on kind ('series' with timedBlocks, 'circuit' with circuit), migrated on read in programStreams.ts, so the compiler flags every site assuming timedBlocks.
   votes: mustFix=5 disputed=0 discs=eng
[critical] #1 TrainHeroic push reads the deleted legacy blocks field and ignores the selected stream
   area: src/server/teamPushPlugin.ts:99-104; src/lib/programStreams.ts:53
   fix: Send streamId in the POST body and resolve the phase server-side through the same streamsOf accessor the UI uses, refusing with a clear 400 when the selected stream has no TrainHeroic team mapped.
   votes: mustFix=2 disputed=0 discs=sc/qa/eng
[high] #2 Push hardcodes Mon/Wed/Fri while the club runs Lower Tuesday and Upper Thursday
   area: src/server/teamPushPlugin.ts:31-35 vs data/schedule.json, data/planning.json
   fix: Derive each session's date from the active schedule scenario: map the session focus to its class type, take the earliest day that class type runs, and show the resolved dates in the confirm dialogue.
   votes: mustFix=2 disputed=0 discs=sc/qa
[high] #6 CircuitEditor keeps its paste buffer across session switches and overwrites the wrong session
   area: src/tabs/programming/ProgrammingTab.tsx:1082; src/tabs/programming/CircuitEditor.tsx:19-54
   fix: Render it as `<CircuitEditor key={session.id} ... />` so the paste buffer is scoped to one session and discarded on switch.
   votes: mustFix=1 disputed=0 discs=eng

=== SHOULD (6) ===
[high] #4 Phases are authored twice, in two tabs, and the shipped data already disagrees
   area: src/tabs/annual/AnnualPlanTab.tsx, src/tabs/programming/ProgrammingTab.tsx, src/seed/index.ts:123-135, data/annual-plan.json vs data/program.json
   fix: Give ProgramBlock an annualPhaseId, resolve names and lengths from the annual lane, add a per-stream pull phases from the annual plan action, and warn in Programming when linked lengths differ.
   votes: mustFix=0 disputed=0 discs=ux/qa/eng/sc
[high] #5 Programming cannot express a no-class window, so dates after Christmas run two weeks early
   area: src/tabs/programming/ProgrammingTab.tsx:121-130, 360-364; data/annual-plan.json:31-35
   fix: Add an optional gapWeeksBefore or nonTraining phase kind to ProgramBlock, include it in the weeksBefore sum, and show it as a greyed strip in the week pills.
   votes: mustFix=0 disputed=0 discs=qa/sc
[high] #11 The board clips silently: exercises fall off the bottom with nothing to show they did
   area: src/tabs/tv/TvPage.tsx:298-348
   fix: Measure the rendered column against the 1080 canvas in a layout effect, step a single font-scale multiplier down until it fits, and show an amber does not fit badge when even the smallest step overflows.
   votes: mustFix=0 disputed=0 discs=ops
[high] #17 Circuit paste fails on the club's own duration-first notation and silently merges pieces
   area: src/lib/circuit.ts:15-22, 60
   fix: Add a duration-first heading pattern covering `8:00 EMOM` and a bare time followed by a keyword, and show a parsed-piece count in the paste panel before committing so a mis-parse is visible.
   votes: mustFix=0 disputed=0 discs=sc
[high] #37 The circuit format cannot carry a load, so Hyrox station weights never reach the floor
   area: src/types/documents.ts:92-97; src/tabs/programming/CircuitEditor.tsx; data/program.json Hyrox sessions
   fix: Add an optional per-line prescription to the circuit model, edited as a small kg field beside each line in CircuitEditor and rendered in sand under the movement on the board.
   votes: mustFix=0 disputed=0 discs=sc
[high] #10 Movement Check judges one phase, hides the stream, and contradicts the rolling two-phase rule
   area: src/tabs/movement/MovementCheckTab.tsx:35-42, 104-107, 142-176; src/types/documents.ts:142-152
   fix: Score coverage across the selected phase and the previous one, relabel the warning accordingly, add a visible stream selector, add muscle-group and trunk-category tag lists, and replace every "block" with "phase".
   votes: mustFix=0 disputed=0 discs=sc/ux/qa

=== NICE (27) ===
[medium] #7 The coach-only note never reaches the wall while coach-only intent headlines it
   area: src/tabs/tv/TvPage.tsx:255-262; src/types/documents.ts:104-106
   fix: Render session.note as a full-width strip under the header at 28px with a sand rule, and demote session.intent to the footer or drop it from the board.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #13 No entry point for today: the app never opens on the week or class actually running
   area: src/components/TabNav.tsx; src/tabs/home/HomeTab.tsx; src/tabs/programming/ProgrammingTab.tsx:75-77; src/tabs/tv/TvPage.tsx:231-247
   fix: Default the Programming selectors to today's phase and week from the annual start date, print the real day and date in the board header, add a /tv/today route, and add a Today panel on Home linking each class to its board, week and layout.
   votes: mustFix=0 disputed=0 discs=ux/ops/sc
[medium] #8 Game Day can be programmed and laid out but cannot exist in the Annual Plan
   area: src/types/documents.ts; src/tabs/annual/AnnualPlanTab.tsx
   fix: Widen AnnualStream['id'] to the shared stream id space, seed a Game Day lane, and build the filter row from data.streams so a new stream appears everywhere at once.
   votes: mustFix=0 disputed=0 discs=ux
[medium] #9 A class has three or four unlinked id namespaces, so nothing cross-links between tabs
   area: src/seed/index.ts:24-34; src/types/documents.ts:126-127, 194-199, 284-290; src/tabs/home/HomeTab.tsx
   fix: Declare one StreamId type used by ProgramStream, LayoutRoom, AnnualStream and RaceEvent, add classTypeIds on ProgramStream (strength to lbs and ubs), and link Schedule and Home blocks through to Programming and Layouts.
   votes: mustFix=0 disputed=0 discs=ux/eng
[medium] #20 Group Fitness Room fixtures are drawn on the Strength layout, and room is free text
   area: src/tabs/layouts/roomModel.ts:1-64; src/tabs/layouts/LayoutsTab.tsx:343-345, 389; data/layouts.json
   fix: Key fixtures by room in a FIXTURES_BY_ROOM record so an unmapped room draws an empty floor, and hold a ScheduleDoc room id in LayoutRoom.room, resolving the display name from the schedule document.
   votes: mustFix=0 disputed=0 discs=qa/ops
[medium] #21 Blocks are hardcoded to fixed four-week pages, so three-week waves and deload weeks cannot be shown
   area: src/tabs/programming/ProgrammingTab.tsx:39, 827-847; src/types/documents.ts:110-113
   fix: Let each phase carry explicit block boundaries defaulting to fours that drive the Block view pages, and add an optional week label shown on the week pills and grid headers.
   votes: mustFix=0 disputed=0 discs=sc
[medium] #22 "Block" means three different things across the app's own surfaces
   area: src/tabs/programming/ProgrammingTab.tsx; src/tabs/tv/TvPage.tsx; PROGRAMMING-PLAN.md
   fix: Rename the TV export to tac-{focus}-phase{n}-week{n} and rewrite the plan document's Block N headings as Phase N, leaving Block for the three to four week window only.
   votes: mustFix=0 disputed=0 discs=ux
[medium] #24 Cues, scales and the blurb are set below the readable floor for a gym-distance screen
   area: src/tabs/tv/TvPage.tsx:335-342, 352-353
   fix: Lift scale lines to at least 24px at 70 percent white with a sand SCALE chip, and drop the paragraph blurb from the board to buy back the vertical space.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #35 Interactive text and timetable card text sit far below any contrast floor
   area: src/tabs/programming/EditableGrid.tsx:210, 483; src/tabs/programming/TimedBlockCard.tsx:80; src/tabs/schedule/ClassBlockCard.tsx:22-27; src/index.css
   fix: Reserve ink-200 and ink-300 for borders, promote every interactive or informational use to ink-500, use solid cream for the class card lines, and darken the two seed colours that fail white at 4.5:1.
   votes: mustFix=0 disputed=0 discs=ux
[medium] #16 SaveBadge shows the most urgent document's state but wires the first document's handlers
   area: src/tabs/home/HomeTab.tsx:92-95; src/tabs/attendance/AttendanceTab.tsx:70-73
   fix: Change SaveBadge to take the doc object so state and handlers cannot be sourced from different documents, and update the two call sites.
   votes: mustFix=0 disputed=0 discs=eng
[medium] #25 The Sheets export renumbers phases across streams and never names the stream
   area: src/lib/exportCsv.ts
   fix: Prefix each week label with its stream and reset the phase counter per stream, for example "ESD P1 W1", and emit a stream name row above each session band.
   votes: mustFix=0 disputed=0 discs=ux
[medium] #33 Race markers are decorative: nothing checks that a race lands on a taper
   area: src/tabs/annual/AnnualPlanTab.tsx:147-177, 508; data/annual-plan.json:167-196
   fix: Resolve the phase each race falls into, draw the pin as a band from date to endDate, and colour the label amber with a title naming the phase whenever the race week is not in a comp, race or taper phase.
   votes: mustFix=0 disputed=0 discs=sc
[medium] #38 Auto-build throws away the load, so the floor plan never says which weight to put out
   area: src/tabs/programming/ProgrammingTab.tsx:638-652; src/lib/circuit.ts equipmentFor
   fix: Have equipmentFor return the matching line alongside each hint, and have buildLayoutForWeek append any kg figure found in that line to the item label, for example "Dumbbells 22.5kg".
   votes: mustFix=0 disputed=0 discs=ops
[medium] #12 Export controls sit permanently over the board header, with no present mode
   area: src/tabs/tv/TvPage.tsx:164-196 vs 239-251
   fix: Auto-hide the control bar after a few seconds of no pointer movement, revealing on mousemove plus a toggle key, and move it to the bottom-left where the footer is quietest.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #14 Programming view coordinates live only in component state, so navigation resets the coach
   area: src/tabs/programming/ProgrammingTab.tsx:75-87, 712
   fix: Move the six coordinates into the URL with useSearchParams, keeping the existing clamping as the parse step, so back, forward, refresh and shared links all land where the coach was.
   votes: mustFix=0 disputed=0 discs=eng
[medium] #18 Renaming a series breaks the board's warm-up strip and overflows the station badges
   area: src/tabs/tv/TvPage.tsx:69, 318-325; src/tabs/programming/TimedBlockCard.tsx:53-58
   fix: Detect the warm-up by a prefix test or a kind flag on TimedBlock rather than exact equality, and cap the badge to the first two characters plus the index so it always fits.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #26 The push refetches the annual plan directly, bypassing the store and duplicating the date maths
   area: src/tabs/programming/ProgrammingTab.tsx:355-364; src/lib/store.ts:1-6
   fix: Delete the inline fetch and derive the push Monday from the already-loaded annual.data.startDate through the single existing mondayOfWeek helper.
   votes: mustFix=0 disputed=0 discs=eng
[medium] #27 ESD is deep pine on Schedule and Home but blue on the Annual Plan, where blue means StretchFit
   area: src/seed/index.ts:25-32, 203-205; src/tabs/home/HomeTab.tsx:290-303
   fix: Drop colour from AnnualStream and resolve lane colours from the matching schedule class type, falling back to a neutral, so one edit recolours every tab.
   votes: mustFix=0 disputed=0 discs=qa
[medium] #28 Auto-build hardcodes three of everything, ignoring the class headcount it already holds
   area: src/tabs/programming/ProgrammingTab.tsx:638-652
   fix: Derive the default count from recent average attendance for that stream, reading the attendance doc as HomeTab does, and expose it as an editable heads number the coach can override before it writes.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #30 The equipment inventory is decorative: nothing checks a layout against what the club owns
   area: src/tabs/equipment/EquipmentTab.tsx:36-38; src/tabs/programming/ProgrammingTab.tsx:647; src/tabs/layouts/roomModel.ts:501-512; src/seed/index.ts:306-321
   fix: Give EquipmentItem an EquipKind keyed to roomModel's kinds, show a gear call panel on Layouts listing used against available with a red badge when over-allocated, and block the auto-build from silently exceeding stock.
   votes: mustFix=0 disputed=0 discs=ops/qa
[medium] #34 Sleds are treated as needing nothing placed, but they are set out and loaded every session
   area: src/lib/circuit.ts:88-90; src/tabs/layouts/roomModel.ts:68-102
   fix: Add a sled EquipKind, palette entry and hint matching sled, push or pull, defaulting to one per lane, and give LayoutItem an optional load in kg rendered on the label, so the plan says Sled 60kg.
   votes: mustFix=0 disputed=0 discs=ops
[medium] #36 focus:outline-none applied with no replacement indicator on several controls
   area: src/tabs/programming/TimedBlockCard.tsx:54; src/tabs/schedule/ScenarioBar.tsx:54; src/tabs/programming/EditableGrid.tsx:131; src/index.css
   fix: Add one rule to index.css, `:focus-visible { outline: 2px solid var(--color-accent-600); outline-offset: 1px; }`, and delete every focus:outline-none from the shared field classes.
   votes: mustFix=0 disputed=0 discs=ux
[medium] #15 An early return pins every handler below the hook boundary, so nothing in Programming can be memoised
   area: src/tabs/programming/ProgrammingTab.tsx:103-660, 998-1015; src/tabs/programming/EditableGrid.tsx:233
   fix: Split into a thin tab that owns useDoc and the loading gate plus a ProgramEditor child where hooks are legal, move pure reducers to src/lib/programEdits.ts, memoise the row model, and hold cell text locally until blur.
   votes: mustFix=0 disputed=0 discs=eng
[medium] #19 No error boundary, and the index clamps can yield undefined on an empty stream or phase list
   area: src/App.tsx:16-46; src/tabs/programming/ProgrammingTab.tsx:111-119; src/lib/programStreams.ts:36
   fix: Add a route-level error boundary rendering a recoverable message, and make the clamps safe with Math.max(0, Math.min(i, len - 1)) plus an explicit empty state when a stream has no phases.
   votes: mustFix=0 disputed=0 discs=eng
[medium] #31 The floor layout builder is pointer-only while announcing a keyboard affordance that does nothing
   area: src/tabs/layouts/LayoutsTab.tsx:129-135, 176
   fix: Register a KeyboardSensor with a coordinateGetter nudging x and y by a fixed step, add an explicit onKeyDown selecting on Enter or Space, and give the item a focus-visible ring matching the selected style.
   votes: mustFix=0 disputed=0 discs=ux
[low] #23 The same object is called Series, Seg and Timed block on three screens
   area: src/tabs/programming/TimedBlockCard.tsx:63; src/tabs/programming/EditableGrid.tsx:265, 422; src/tabs/programming/ProgrammingTab.tsx:1126
   fix: Rename the button to "+ Series" and both grid headers to "Series", keeping timedBlocks as the internal field name only.
   votes: mustFix=0 disputed=0 discs=qa
[low] #29 Planning blocks on a document it never reads, and the type file documents an editor that does not exist
   area: src/tabs/planning/PlanningTab.tsx:76, 82-84; src/types/documents.ts:263-264
   fix: Delete the useDoc('program') call, its clause in the loading gate and the stale comment, or implement the editor, and rename the two sections to Captured notes and Scratchpad.
   votes: mustFix=0 disputed=0 discs=ux/eng/qa

=== SYNTHESIS ===
# TAC Coaching Tool: Review Report

Five of five discipline leads returned a ballot and all five met quorum coverage. Every item below was confirmed against the files by the adversarial verification pass. The panel recorded no scoring disagreement (no spreads of 3 or more, no dissent notes), so the ranking below is uncontested.

## Must-fix now

1. **TV board, grids and CSV render nothing for ESD, Hyrox and Game Day** (`src/tabs/tv/TvPage.tsx:105-107`, `src/tabs/programming/EditableGrid.tsx:65-98`, `src/lib/exportCsv.ts:43`). Three of the four streams are written as circuits and every output surface only handles series, so on those days the wall is blank. Add a circuit branch to TvPage that renders each CircuitBlock heading, its lines and restAfter as board cards, emit circuit text as CSV rows, and give the Block and Phase grids a read-only circuit summary.
2. **Session carries two mutually exclusive payloads with no discriminator** (`src/types/documents.ts:99-108`, `src/tabs/programming/ProgrammingTab.tsx:134, 168-194`). Writers assume `timedBlocks` and silently drop circuits, which is the root cause of item 1 and will keep recurring. Make Session a discriminated union on `kind` ('series' with timedBlocks, 'circuit' with circuit), migrated on read in `programStreams.ts`, so the compiler flags every unsafe site.
3. **TrainHeroic push reads the deleted legacy blocks field and ignores the selected stream** (`src/server/teamPushPlugin.ts:99-104`, `src/lib/programStreams.ts:53`). The push either sends nothing or sends the wrong stream's work, which is the one action Chris cannot check before it lands. Send `streamId` in the POST body, resolve the phase server-side through the same `streamsOf` accessor the UI uses, and refuse with a clear 400 when the selected stream has no TrainHeroic team mapped. Drafts only, as now.
4. **Push hardcodes Mon/Wed/Fri while the club runs Lower Tuesday and Upper Thursday** (`src/server/teamPushPlugin.ts:31-35` versus `data/schedule.json`, `data/planning.json`). Sessions land on days the class does not run, so athletes see the wrong work on the wrong day. Derive each session's date from the active schedule scenario, mapping the session focus to its class type and taking the earliest day that class type runs, and show the resolved dates in the confirm dialogue.
5. **CircuitEditor keeps its paste buffer across session switches** (`src/tabs/programming/ProgrammingTab.tsx:1082`, `src/tabs/programming/CircuitEditor.tsx:19-54`). Paste into one session, switch, commit, and you overwrite the wrong session's work. Render it as `<CircuitEditor key={session.id} ... />` so the buffer is scoped to one session and discarded on switch.

## Should-fix

6. **Phases are authored twice in two tabs and the shipped data already disagrees** (`AnnualPlanTab.tsx`, `ProgrammingTab.tsx`, `src/seed/index.ts:123-135`, `data/annual-plan.json` versus `data/program.json`). Two sources of truth for the 10/1/6 week structure means the annual ruler and the programming weeks drift apart. Give ProgramBlock an `annualPhaseId`, resolve names and lengths from the annual lane, add a per-stream pull-phases-from-annual action, and warn when linked lengths differ.
7. **Programming cannot express a no-class window, so dates after Christmas run two weeks early** (`ProgrammingTab.tsx:121-130, 360-364`, `data/annual-plan.json:31-35`). Every week label after the shutdown is wrong. Add an optional `gapWeeksBefore` or a nonTraining phase kind to ProgramBlock, include it in the weeksBefore sum, and show it as a greyed strip in the week pills.
8. **The TV board clips silently: exercises fall off the bottom with nothing to show they did** (`TvPage.tsx:298-348`). A coach on the floor cannot tell the board is incomplete. Measure the rendered column against the 1080 canvas in a layout effect, step a single font-scale multiplier down until it fits, and show an amber "does not fit" badge when even the smallest step overflows.
9. **Circuit paste fails on the club's own duration-first notation and silently merges pieces** (`src/lib/circuit.ts:15-22, 60`). `8:00 EMOM` parses as part of the previous piece, so the session is quietly wrong. Add a duration-first heading pattern covering a bare time followed by a keyword, and show a parsed-piece count in the paste panel before committing.
10. **The circuit format cannot carry a load, so Hyrox station weights never reach the floor** (`src/types/documents.ts:92-97`, `CircuitEditor.tsx`, Hyrox sessions in `data/program.json`). Coaches have to guess or ask. Add an optional per-line prescription, edited as a small kg field beside each line, rendered in sand under the movement on the board.
11. **Movement Check judges one phase, hides the stream, and contradicts the rolling two-phase rule** (`MovementCheckTab.tsx:35-42, 104-107, 142-176`, `src/types/documents.ts:142-152`). The coverage warning fires against the wrong window and against an unnamed stream, so it cannot be trusted. Score across the selected phase and the previous one, relabel the warning, add a visible stream selector, add muscle-group and trunk-category tag lists, and replace every "block" with "phase".

## Nice-to-have

Top 15 of 27 by rank. Twelve further items are omitted from this report.

12. **The coach-only note never reaches the wall while coach-only intent headlines it** (`TvPage.tsx:255-262`). Render `session.note` as a full-width 28px strip under the header with a sand rule, and demote or drop `session.intent`.
13. **No entry point for today: the app never opens on the week or class actually running** (`TabNav.tsx`, `HomeTab.tsx`, `ProgrammingTab.tsx:75-77`, `TvPage.tsx:231-247`). Default the Programming selectors to today's phase and week, print the real day and date in the board header, add a `/tv/today` route, and add a Today panel on Home.
14. **Game Day can be programmed and laid out but cannot exist in the Annual Plan** (`src/types/documents.ts`, `AnnualPlanTab.tsx`). Widen `AnnualStream['id']` to the shared stream id space, seed a Game Day lane, and build the filter row from `data.streams`.
15. **A class has three or four unlinked id namespaces, so nothing cross-links between tabs** (`src/seed/index.ts:24-34`, `src/types/documents.ts:126-127, 194-199, 284-290`, `HomeTab.tsx`). Declare one StreamId type shared by ProgramStream, LayoutRoom, AnnualStream and RaceEvent, add `classTypeIds` on ProgramStream, and link Schedule and Home through to Programming and Layouts.
16. **Group Fitness Room fixtures are drawn on the Strength layout, and room is free text** (`roomModel.ts:1-64`, `LayoutsTab.tsx:343-345, 389`, `data/layouts.json`). Key fixtures by room in a `FIXTURES_BY_ROOM` record so an unmapped room draws an empty floor, and hold a ScheduleDoc room id in `LayoutRoom.room`.
17. **Blocks are hardcoded to fixed four-week pages, so three-week waves and deload weeks cannot be shown** (`ProgrammingTab.tsx:39, 827-847`, `src/types/documents.ts:110-113`). Let each phase carry explicit block boundaries defaulting to fours, and add an optional week label on the pills and grid headers.
18. **"Block" means three different things across the app's own surfaces** (`ProgrammingTab.tsx`, `TvPage.tsx`, `PROGRAMMING-PLAN.md`). Rename the TV export to `tac-{focus}-phase{n}-week{n}` and rewrite the plan document's Block N headings as Phase N, leaving Block for the three to four week window only.
19. **Cues, scales and the blurb are set below the readable floor for a gym-distance screen** (`TvPage.tsx:335-342, 352-353`). Lift scale lines to at least 24px at 70 per cent white with a sand SCALE chip, and drop the paragraph blurb to buy back vertical space.
20. **Interactive text and timetable card text sit far below any contrast floor** (`EditableGrid.tsx:210, 483`, `TimedBlockCard.tsx:80`, `ClassBlockCard.tsx:22-27`, `src/index.css`). Reserve ink-200 and ink-300 for borders, promote interactive and informational text to ink-500, use solid cream for class card lines, and darken the two seed colours failing white at 4.5:1.
21. **SaveBadge shows the most urgent document's state but wires the first document's handlers** (`HomeTab.tsx:92-95`, `AttendanceTab.tsx:70-73`). Change SaveBadge to take the doc object so state and handlers cannot diverge, and update both call sites.
22. **The Sheets export renumbers phases across streams and never names the stream** (`src/lib/exportCsv.ts`). Prefix each week label with its stream and reset the phase counter per stream, for example "ESD P1 W1", and emit a stream name row above each session band.
23. **Race markers are decorative: nothing checks that a race lands on a taper** (`AnnualPlanTab.tsx:147-177, 508`, `data/annual-plan.json:167-196`). Resolve the phase each race falls into, draw the pin as a band from date to endDate, and colour the label amber when the race week is not in a comp, race or taper phase.
24. **Auto-build throws away the load, so the floor plan never says which weight to put out** (`ProgrammingTab.tsx:638-652`, `equipmentFor` in `src/lib/circuit.ts`). Have `equipmentFor` return the matching line with each hint, and append any kg figure to the item label, for example "Dumbbells 22.5kg".
25. **Export controls sit permanently over the board header, with no present mode** (`TvPage.tsx:164-196` versus `239-251`). Auto-hide the control bar after a few seconds of no pointer movement, reveal on mousemove plus a toggle key, and move it to the quiet bottom-left.
26. **Programming view coordinates live only in component state, so navigation resets the coach** (`ProgrammingTab.tsx:75-87, 712`). Move the six coordinates into the URL with `useSearchParams`, keeping the existing clamping as the parse step.

## Highest-leverage change

**Make Session a discriminated union on `kind` (item 2).** It is the single structural defect behind the tool's worst failure: three of four streams rendering nothing on the wall, in the grids and in the CSV. Fixing the type first turns item 1 from a hunt into a compiler-guided sweep, and it stops the same class of silent drop reappearing in every new writer. Do it before the TrainHeroic push fixes, then ship items 3 and 4 together so the one irreversible-feeling action, pushing drafts to athletes, is correct on both content and date.
